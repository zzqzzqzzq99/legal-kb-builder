# ─── 必需依赖（所有功能都需要） ───
PyYAML>=6.0

# ─── 强烈建议（四条流水线通用，装了检索能力大幅提升） ───
# 若完全不装，hybrid_search 仅能做"结构索引 + Grep"单路检索
pypdf>=3.0.0          # PDF 拆分用
jieba>=0.42.1          # 中文分词（BM25 前置）
rank-bm25>=0.2.2       # BM25 关键词检索
numpy>=1.24.0          # 数值计算
